<h1>編集画面（変更必要）</h1>

<%= form_with model: @book do |f| %>

  <h4>タイトル（変更必要）</h4>
  <%= f.text_area :body %>
  
  <h4>本文（変更必要）</h4>
  <%= f.text_area :body %>
  
  <%= f.submit '保存（変更必要）' %>
  
<% end %>
