<div>
  <%= flash[:notice] %>
</div>

<h1>タイトル</h1>
<p><%=@book.title %></p>

<h2>本文</h2>
<p><%=@book.body %></p>


<%= link_to "編集（変更必要）", edit_book_path(@book.id) %>
<%= link_to "削除（変更必要）", book_path(@book.id), method: :delete, "data-cofirm" => "本当に削除しますか？（変更必要）" %>
